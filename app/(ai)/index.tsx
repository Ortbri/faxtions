import React from 'react';
import { Text, View } from 'react-native';

/**
 * AI Chat
 * this will be a RSC that ai will be able to create its own json workout components and automatic track your workout without having to manually input the data
 * if you need a workout or have an idea for the workout, and have to generate the rest, then it helps and creates that as a workout, then it can return some examples
 * from tiktok or youtube
 * do we need to create a api of full workouts or how workouts should be done?
 */

export default function AiChat() {
  return (
    <View>
      <Text>index</Text>
    </View>
  );
}
